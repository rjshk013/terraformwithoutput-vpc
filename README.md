# terraformwithoutput-vpc

We can create a manually controlled vpc & public ,private subnets using this tf files.Need to modify region,azs,vpc cidr block,each public & private subnets cidr blocks .Also manage how to launch instances in public * private subnets with particular azs and number of instances per launch.Also outputs public ips of instances 
